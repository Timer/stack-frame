{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --npm-client=yarn",
    "publish": "lerna publish",
    "start": "concurrently -k \"cd packages/stack-frame && yarn build -- -w\" \"cd packages/stack-frame-parser && yarn build -- -w\" \"cd packages/stack-frame-resolver && yarn build -- -w\"",
    "format": "prettier --single-quote --trailing-comma es5 --write 'packages/*/src/**/*.js' --write 'packages/*/test/**/*.js'",
    "test": "tasks/test.sh"
  },
  "devDependencies": {
    "concurrently": "^3.4.0",
    "lerna": "2.0.0-beta.38",
    "prettier": "^0.21.0"
  }
}
